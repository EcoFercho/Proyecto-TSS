services:
  - type: web
    name: simulador-ahorros
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT interfaz:app.server"
    plan: free          # opcional
